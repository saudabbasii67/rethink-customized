package com.celzero.bravedns.ui

import android.content.pm.PackageManager

open class FragmentDialog {
//    fun showPermissionRequestDialog() {
//        // Create a new instance of the PermissionRequestFragmentDialog class.
//        val dialog = PermissionRequestFragmentDialog()
//
//        // Show the dialog.
//        dialog.show(supportFragmentManager(), "PermissionRequestDialog")
//    }
//    override fun onRequestPermissionsResult(
//        requestCode: Int,
//        permissions: Array<out String>,
//        grantResults: IntArray
//    ) {
//        // Check if the request code matches the request code for the permission request dialog.
//        if (requestCode == 100) {
//            // Check if the permission was granted.
//            if (grantResults.isNotEmpty() && grantResults[0] == PackageManager.PERMISSION_GRANTED) {
//                // The permission was granted. Proceed with whatever action requires superuser permission.
//            } else {
//                // The permission was denied. Take appropriate action, such as displaying an error message or disabling the feature that requires superuser permission.
//            }
//        }
//    }

}
